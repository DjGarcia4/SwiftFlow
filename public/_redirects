# A single-page app: any path that isn't a file gets the app, which routes
# it. Files that exist (sobre.html, historial.html, assets) are served
# first, so this only catches the rest.
/*    /index.html   200
